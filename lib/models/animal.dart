@immutable
import 'package:flutter/material.dart';

class Animal {
  final int id;
  final String name;
  final String picture;
  final String soundfile;

  Animal(this.id, this.name, this.picture, this.soundfile);
}
